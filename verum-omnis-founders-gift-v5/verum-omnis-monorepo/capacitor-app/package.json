{
  "name": "verum-capacitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && cp -r ../web/* www/",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.0.0"
  }
}
