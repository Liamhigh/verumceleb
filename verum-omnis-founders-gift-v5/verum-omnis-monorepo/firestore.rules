rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow server-side access to receipts collection
    // This is intentionally restrictive since receipts are managed by functions only
    match /receipts/{receiptId} {
      allow read, write: if false; // Block all client access
    }
  }
}